# Phase 0 — Project Setup & Scaffold

## Goal

Initialize a Next.js 16 App Router project with TypeScript, Tailwind CSS v4,
and all tooling required to match the reference repo's architecture. Configure
GitHub Pages static export so the site can deploy from the `main` branch.

---

## Problem Statement

Before any design or content work begins, the project needs a verified, working
scaffold. Without this foundation, later phases cannot be built reliably.

This phase has no visible output to a visitor. Its job is entirely structural:
create the environment where everything else can be built correctly.

---

## Deliverables

- [ ] Next.js 16 project initialized with App Router and strict TypeScript
- [ ] Tailwind CSS v4 configured with PostCSS
- [ ] ESLint configured (Next.js recommended rules)
- [ ] Prettier configured for formatting consistency
- [ ] `next.config.ts` set for static export (`output: 'export'`)
- [ ] `tsconfig.json` with path alias `@/` pointing to project root
- [ ] `.gitignore` covers `node_modules`, `.next`, `out`
- [ ] GitHub repository initialized and connected to remote
- [ ] Initial commit pushed to `main`
- [ ] GitHub Pages enabled on repository settings

---

## Architecture Decisions

### Static Export

The site uses `output: 'export'` in `next.config.ts`. This produces a static
`out/` directory that GitHub Pages can serve directly. No server-side rendering
or API routes are needed for a portfolio.

### Path Aliases

All internal imports use `@/` as the root alias. This matches the reference
repo and keeps imports clean across deeply nested component files.

```ts
// tsconfig.json
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./*"]
    }
  }
}
```

### No `src/` Directory

Files live directly at the root level: `app/`, `components/`, `lib/`,
`public/`. This matches the reference repo structure exactly.

---

## Folder Structure Target

```
IS117-Portfolio-Project/
├── app/
│   ├── globals.css
│   ├── layout.tsx
│   └── page.tsx
├── components/
│   ├── ui/
│   └── site/
├── lib/
│   ├── site.ts
│   └── site-navigation.ts
├── public/
│   └── media/
├── docs/
│   └── specs/
├── next.config.ts
├── tsconfig.json
├── tailwind.config.ts
├── postcss.config.mjs
├── eslint.config.mjs
├── .prettierrc
├── .gitignore
├── package.json
└── README.md
```

---

## Commands

```bash
# Initialize
npx create-next-app@latest . --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"

# Verify
npm run typecheck
npm run lint
npm run build
```

---

## Quality Gates

- [ ] `npm run dev` starts without errors
- [ ] `npm run build` produces `out/` directory
- [ ] `npm run typecheck` passes with zero errors
- [ ] `npm run lint` passes with zero errors
- [ ] GitHub remote is connected
- [ ] GitHub Pages is configured and accessible

---

## Notes for AI Workflow

This phase uses minimal AI assistance. The scaffold is generated by
`create-next-app` and then manually configured. AI was used to:

- Confirm the correct `next.config.ts` settings for static export
- Verify Tailwind v4 PostCSS configuration

Human judgment was applied to:

- Folder naming and structure decisions
- Path alias setup
- GitHub Pages branch strategy
